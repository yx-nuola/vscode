{
  "rows": 64,
  "cols": 64,
  "cells": []
}

// Generate test data
for (let bl = 0; bl < 64; bl++) {
  for (let wl = 0; wl < 64; wl++) {
    const imeas = Math.random() * 20;
    const status = imeas > 15 ? 'fail' : 'pass';
    data.cells.push({
      bl,
      wl,
      vset: Math.random() * 5,
      vreset: Math.random() * 3,
      imeas: imeas,
      status: status
    });
  }
}
